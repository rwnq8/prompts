# ═══════════════════════════════════════════════════════════════
# TIER 2 AGENT DEFAULTS — IMMUTABLE DIRECTIVE
# ═══════════════════════════════════════════════════════════════
# Effective:  2026-05-07
# Directive:  META-PROMPT-DEEPSEEK v2.0
# Scope:      ALL Tier 2 prompts/agents generated from this workspace
# Status:     PERMANENT (supersedable only by explicit user directive)
#
# NOTE: DeepChat does not expose a temperature slider for the
# deepseek-v4-pro model in its UI. Therefore, this directive is
# enforced at the PROMPT GENERATION LAYER — every Tier 2 system
# prompt produced by the Tier 1 compiler MUST embed the following
# parameters in its configuration block:
#
#   temperature: 0.0
#
# RATIONALE: Deterministic, reproducible output. Zero randomness.
# All Tier 2 agents operate as precision compilers with no
# stochastic variance in their generation pipeline.
# ═══════════════════════════════════════════════════════════════

DEFAULT_TEMPERATURE=0.0
DEFAULT_TOP_P=1.0
DEFAULT_FREQUENCY_PENALTY=0.0
DEFAULT_PRESENCE_PENALTY=0.0

# ENFORCEMENT: Every Tier 2 prompt file generated by the Tier 1
# compiler MUST include the following block (verbatim) unless a
# superseding user directive overrides it:
#
#   CONFIGURATION:
#     temperature: 0.0
#     top_p: 1.0
#     frequency_penalty: 0.0
#     presence_penalty: 0.0
