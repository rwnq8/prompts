# HANDOFF — Session Closeout 2026-05-27

> **Multi-Agent Handoff:** This document delegates tasks to three agents: Projects Agent, Prompts Agent, and QWAV/Program Agent. Each agent reads its respective section on session start.

**Date:** 2026-05-27
**Issuing Authority:** System Prompt Generator (session terminating)
**Git Branch:** `main` (4 commits ahead of `origin/main`)
**Last Commit:** `9142d78` — `ACTION:EDIT FILE: DEFAULT.md RATIONALE:Add sandbox-token-unavailable (403) failure mode to §11.8.3`

---

## 1. PROJECTS AGENT — Independence Fallacy Publication Pipeline

**Context:** The Independence Fallacy paper (`0.1.md`, 41,935 B) passed all gates: 2-round reader testing, cross-project synthesis audit (§11.6), 1,000-run simulation verified. It's publication-ready.

**State:** PDF generated locally (628,969 bytes ✓). Git has 2 local commits from prior session (API-uploaded to GitHub but local history needs sync). Zenodo sandbox returned 403 (separate token required) — use `[SANDBOX-SKIPPED]` per §11.8.3 and proceed to production.

### Execute:

1. **Sync git.** `git pull --rebase origin feature/initial-setup` (or whichever branch the paper is on). Push any remaining local commits. If rebase conflicts, resolve and force consistency.

2. **Register DOI.** `ZENODO_TOKEN` is set. Run production directly:
   ```powershell
   python "G:\My Drive\prompts\tools\zenodo_publish.py" --token $env:ZENODO_TOKEN --title "The Independence Fallacy: The Hidden Axiom in All Hierarchical Models" --author "Quni-Gudzinas, Rowan Brad" --file "0.1.md" --abstract "<extract from paper>" --keywords "hierarchical models, independence axiom, baseline convergence, cophenetic distance, ultrametric, phylogenetics, Lyapunov exponent, shared causal past" --upload-type publication
   ```
   Sandbox skipped per §11.8.3: `[SANDBOX-SKIPPED: separate sandbox token required at https://sandbox.zenodo.org/account/settings/applications/]`

3. **Update manuscript.** Replace placeholder DOI in `0.1.md` frontmatter with real DOI from API response. Verify with Python regex — no `########` remains.

4. **Create GitHub Release.** `gh release create v1.0.0 --repo qnfo/<repo> --title "v1.0.0 — The Independence Fallacy" --notes "Initial preprint release. DOI: <real_doi>"`. Attach both `0.1.md` AND the PDF.

5. **Social media.** Invoke `SOCIAL-ORCHESTRATOR-TEMPLATE` via `fill_prompt_template` with the paper metadata. Post via Buffer API (`create_post`) for Bluesky, Twitter/X, Mastodon, LinkedIn. DO NOT post before DOI is registered (§11.8.5).

6. **Close out.** Update project-state GitHub Issue. Commit all changes. Merge to main. Delete feature branch.

---

## 2. PROMPTS AGENT — System Maintenance

**Context:** `main` has 4 commits ahead of `origin/main` from the `feature/automate-zenodo-social-merge` work:
- `1beb07a` CLOUDFLARE-DEPLOYMENT v2.0
- `b416d73` §11.8 Zenodo DOI + SOCIAL-ORCHESTRATOR v1.1
- `b8bc087` prompts.json rebuild
- `9142d78` §11.8.3 sandbox-token-unavailable fix (applied during this session from a discovered protocol dead-end)

### Execute:

1. **Push to origin.** `git push origin main`. Verify with `git log origin/main..main --oneline` (should return empty).

2. **Verify §11.8.3 fix.** Read `DEFAULT.md` §11.8.3, confirm the new row "Sandbox token unavailable (403)" is present and coherent. The fix allows production fallback when sandbox token is unavailable — tested-by-discovery via the Projects agent's 403 experience.

3. **CLOUDFLARE-CLOSEOUT-2026-05-27.md.** This file is untracked in the prompts directory. It's a QWAV agent closeout report documenting the Cloudflare migration (14 domains, DNS records, redirect rules, \$0 cost). Not a prompt/template — move to archive or leave for QWAV agent to handle. Do NOT commit to prompts repo.

4. **System health.** Run `tools/system_audit.py`. Expected: 6 PASS, 1 WARN (CROSS-PROJECT-LEARNINGS.md on wiki — expected), 0 FAIL. No action needed on the WARN.

5. **Cross-Project Learnings.** If the sandbox-token discovery (§11.8.3 gap) warrants a wiki entry, add it to [Cross-Project Learnings](https://github.com/rwnq8/prompts/wiki/Cross-Project-Learnings) as a new lesson.

---

## 3. QWAV/PROGRAM AGENT — Portfolio & Strategy

**Context:** Cloudflare migration completed (✓, see `CLOUDFLARE-CLOSEOUT-2026-05-27.md` in prompts directory). 14 domains resolve, DNS and redirect rules deployed, \$0 monthly cost. CLOUDFLARE-DEPLOYMENT template updated to v2.0.

### Deferred items (Phase 2/3):

| # | Item | Phase | Notes |
|:--|:-----|:------|:------|
| 1 | rwnq8 Pages migration (9 sites) | 2 | On hold per user instruction |
| 2 | Google Site fixes (qwav.tech, qnfo.org, q08.org) | 2 | AI Studio App Builder fixes needed |
| 3 | Template registration (CLOUDFLARE-DEPLOYMENT) | 2 | "System reload needed" — verify `prompts.json` already has it (entry #2, rebuilt) |
| 4 | Vectorize index populated | 3 | Document ingestion pipeline required |
| 5 | Email Workers | 3 | Dashboard config needed |
| 6 | Email routing verification | 2 | API query + Dashboard check |
| 7 | R2 bucket inventory (`0pus`, `mail`) | 2 | Not accessed in migration session |
| 8 | Security headers (archive + ask-qwav) | 2 | Low priority |
| 9 | Custom 404 pages | 2 | Low priority |
| 10 | Domain expiry WHOIS | 2 | External registrar check |
| 11 | QNFO/QWAV full issue list in archive | 2 | QNFO flagging blocks API |
| 12 | `emailqueue` configuration audit | 2 | 0 producers/consumers since 2024 |

### Execute:

1. **Pick up CLOUDFLARE-CLOSEOUT.** Read `G:\My Drive\prompts\CLOUDFLARE-CLOSEOUT-2026-05-27.md`. This is your session closeout — all decisions, DNS records, redirect rules, procedures, and failure modes are documented there. Move it to your project directory or archive.

2. **Verify template registration.** Check that CLOUDFLARE-DEPLOYMENT v2.0 is registered in `prompts.json` (should be — rebuilt during this session). If `fill_prompt_template("CLOUDFLARE-DEPLOYMENT", ...)` works, the "system reload" is done.

3. **Prioritize Phase 2.** The rwnq8 Pages migration and Google Site fixes are the highest-impact deferred items. QNFO flagging issue may block API access to issues — coordinate with user.

4. **Independence Fallacy paper.** Monitor the Projects agent's publication pipeline. Once the paper is published (DOI registered, GitHub Release created, social media posted), update the QNFO Program Board.

5. **Program board hygiene.** Review open GitHub Issues across qnfo/\* repos. Close any stale items. Ensure project-state Issues are current.

---

*Handoff generated 2026-05-27 by System Prompt Generator v4.6. Each agent begins its session by reading its respective section above.*
