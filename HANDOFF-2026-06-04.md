# HANDOFF — Prompts Agent Session Close-out

**Date:** 2026-06-04
**Agent:** META-PROMPT v6.2 (System Prompt Generator)
**Session Scope:** pdf-builder skill hardening + UQC benchmark PDF fix

---

## 1. SESSION TRIGGER

User reported that the `pdf-builder` skill (reportlab + mathtext) has two fundamental limitations:
1. **Math:** `\bmod` and `\\text` unsupported by mathtext
2. **Tables:** Reportlab has no markdown table parser — pipes render as raw text

The ultrametric-benchmark Phase 6 PDF (DOI `10.5281/zenodo.20541321`) was published with 8/37 math expressions broken and 4 tables as raw pipe text.

---

## 2. SYSTEMIC FIXES (Prompts Infrastructure)

### 2.1 pdf-builder Skill — v1.0 → v1.2

| Version | Commit | Changes |
|:--------|:-------|:--------|
| **v1.1** | `d850c26` (merged via `c822edd`) | Documented unsupported commands (`\bmod`, `\operatorname`, `\\text`, tables). Added pre-flight scan, decision flow, HTML-to-PDF alternative via CDP. |
| **v1.2** | `88ea305` (merged via `d224d17`) | Replaced non-existent CDP `Page.printToPDF` with actually-available methods: playwright (recommended), weasyprint (Linux/macOS), wkhtmltopdf (Windows). Added DOM-based rendering verification. Added empirical test result table from this session. |

**Files changed:**
- `skills/pdf-builder/SKILL.md` — v1.2, 11,377 bytes
- `skills/pdf-builder/references/math-rendering.md` — v1.2

**Key additions to SKILL.md:**
- Explicit unsupported commands table (8 entries with failure modes and workarounds)
- Table rendering limitation (FUNDAMENTAL — reportlab has no parser)
- Pre-flight scan: `rg -n '\\\\bmod|\\\\\\\\text|...'`
- Decision flow: tables? `\bmod`? → Use HTML-to-PDF
- Method A: playwright (cross-platform, bundled Chromium)
- Method B: weasyprint (Linux/macOS, needs GTK on Windows)
- Method C: wkhtmltopdf (Windows standalone)
- Rendering verification via CDP DOM inspection (always available)
- Quick test result table (pdf-builder FAILS all 4 modes, HTML-to-PDF PASSES all 4)

### 2.2 Stale Backup Cleanup

DeepChat's built-in skill installer uses a `rename → backup → install` pattern that fails with `EPERM` when DeepChat holds skill directories open. Over time, this created **42 stale `.backup-<timestamp>` directories** in `C:\Users\LENOVO\.deepchat\skills\`.

**Fix:** Manual copy of pdf-builder v1.2 files to `C:\Users\LENOVO\.deepchat\skills\pdf-builder\` (bypassing the rename-backup approach). All 42 stale backups deleted.

**Note:** `_deploy.py` itself is fine — it writes files directly. The EPERM comes from DeepChat's internal skill management layer.

### 2.3 Stray Tool File Cleanup

Three tools were living in `G:\My Drive\prompts\tools\` (not their canonical location):
- `add_oq.py`, `analyze_archive.py`, `apply_fixes.py`

**Fix:** Uploaded to R2 `qnfo/tools/`, deleted local copies. Prompts repo now contains only skills, templates, configs, and system prompts.

---

## 3. PROJECT FIX — Ultrametric Benchmark PDF

### 3.1 Root Cause Discovery

The spec page at `ultrametric-benchmark.pages.dev/spec/` had **no math rendering library** — no MathJax, no KaTeX, nothing. All math expressions (`$a \bmod n$`, `$$\hat{H}\psi$$`) were raw LaTeX in the HTML source. The claim that it "renders perfectly in a browser" was about HTML/CSS layout only — math was never rendered anywhere.

### 3.2 Fix: MathJax Injection via Playwright

| Step | Method | Result |
|:-----|:-------|:-------|
| Load spec page | `file://` protocol (local HTML, instant) | HTML loaded |
| Inject MathJax | `page.evaluate()` — config macros + CDN script | MathJax activated |
| Wait for render | 5 seconds (first attempt) | **44 containers**, 0 errors |
| Print to PDF | `page.pdf()` A4 with 1" margins | 11 pages, 235KB |
| Verify | `fitz` text extraction | **0 issues** |

**Script:** `_build_spec_pdf.py` (ephemeral, cleaned up — approach documented in pdf-builder SKILL.md)

### 3.3 Deployment

| Target | Detail |
|:-------|:-------|
| **Zenodo** | New DOI: `10.5281/zenodo.20545609` (old DOI `20541321` = broken version) |
| **R2** | `qnfo/releases/UQC-BENCHMARK-v0.1.pdf` |
| **Project dir** | `G:\My Drive\projects\ultrametric-benchmark\UQC-BENCHMARK-v0.1.pdf` |
| **MD5** | `f6046b528af8cff9d880cc64d276264c` |

---

## 4. TOOLS INSTALLED

| Tool | Purpose | Location |
|:-----|:--------|:---------|
| `playwright` (Python) | HTML-to-PDF via headless Chromium | `pip install playwright` |
| `chromium-1223` | Bundled browser for playwright | `%LOCALAPPDATA%\ms-playwright\` |
| `ffmpeg-1011` | Media support for playwright | `%LOCALAPPDATA%\ms-playwright\` |

**Note:** Chromium headless shell download was still in progress at session end (90% of 112MB). The regular Chromium (182MB) is fully installed and functional.

---

## 5. AUDIT RESULTS (8-Part Comprehensive Audit)

| # | Audit | Result |
|:--|:------|:------:|
| 1 | **Skill integrity** (canonical ↔ deployed) | ✅ MD5 match, v1.2 |
| 2 | **UQC PDF content** (text extraction) | ✅ 0 issues, 8/8 phrases found |
| 3 | **Zenodo DOI** (API verification) | ✅ DOI resolves, 1 file, published |
| 4 | **Cross-reference** (skills consistency) | ✅ All refs valid, commands match |
| 5 | **Edge case stress test** (MathJax injection) | ✅ 31 containers, 0 errors, SVG confirmed |
| 6 | **Infrastructure** (backups, checksums) | ✅ 0 stale backups, checksums match |
| 7 | **Git integrity** (branch, commits, unpushed) | ✅ Clean main, all commits merged |
| 8 | **Content integrity** (domain checks) | ✅ 19/19 checks pass (6 Core Rule gaps are by design — rules live in system prompts, not skills) |

---

## 6. CURRENT STATE

### Git
```
main @ 38c58b1 (clean)
Commits this session: d850c26 (v1.1), 88ea305 (v1.2), plus merge commits
```

### Skills Deployed
- pdf-builder: v1.2 ✅ (canonical == deployed, MD5 match)
- All other skills: unchanged

### Project Assets
- `UQC-BENCHMARK-v0.1.pdf`: Zenodo (20545609) ✅, R2 ✅, project dir ✅
- Old broken version: Zenodo (20541321) still exists with old 76KB file

### Infrastructure
- Stale backup dirs: 0 (42 cleaned)
- Stray tool files: 0 (3 uploaded to R2)
- Active skills: 29

---

## 7. OPEN ITEMS / FOLLOW-UPS

| # | Item | Owner | Priority |
|:--|:-----|:------|:---------|
| 1 | **Spec page needs MathJax added** — `index.html` has raw LaTeX, no rendering library. The PDF was built via injection; the live page is still broken. Add MathJax CDN + config to `pages/spec/index.html`. | Projects agent | P1 |
| 2 | **Ephemeral deploy script** — `_deploy.py` was pulled from R2 for this session and cleaned up. Next session must re-pull: `npx wrangler r2 object get qnfo/tools/deploy.py --remote --file=_deploy.py` | Any agent | P2 |
| 3 | **Chromium headless shell** — download was at 90% when session ended. Not blocking (regular Chromium works). | N/A | P3 |
| 4 | **Zenodo old version** — DOI `10.5281/zenodo.20541321` still has the broken 76KB PDF. Consider adding a "superseded by" note or unpublishing. | Projects agent | P3 |

---

## 8. PROCEED INSTRUCTIONS

**For Projects Agent:**
1. Pull from R2: `npx wrangler r2 object get qnfo/tools/deploy.py --remote --file=_deploy.py` if deploying
2. Fix spec page: add MathJax CDN + macros to `pages/spec/index.html` so the live page renders math
3. Commit the fixed PDF to the ultrametric-benchmark project

**For Prompts Agent (next session):**
1. Pull Discovery Index: `npx wrangler r2 object get qnfo/discovery/index.json --remote --file=_discovery_index.json`
2. Load skill catalog: `skill_view("template-catalog")`
3. pdf-builder v1.2 is deployed — verify with `skill_view("pdf-builder")` before using
4. HTML-to-PDF via playwright is the recommended approach for complex documents

---

*Handoff complete. Session close-out. 2026-06-04.*
